sudo: required
dist: trusty
language: java
jdk:
  - oraclejdk8
before_install:
  - 'npm i -g n grunt' # Install NodeJS deps
  - 'sudo n lts' # Bump NPM to LTS version
install:
  - 'cd app && npm i'
  - 'cd ..'
script:
  - 'cd spigot && mvn test'
  - 'cd ../app && npm test'
  - 'cd ..'
